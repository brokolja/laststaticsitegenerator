{
  "name": "laststaticsitegenerator",
  "version": "1.0.5",
  "description": "The LastStaticSiteGenerator combines the best tools for the job to simplify your frontend-developer live. Zero configuration! Hot-reloading everything!",
  "private": true,
  "scripts": {
    "clean": "rm -rf ./dist ./.cache ./cache_parcel ./cache_eleventy ",
    "dev": "npm run clean && npm run production:eleventy && npm run message:dev && run-p --race dev:eleventy dev:parcel",
    "dev:https": "npm run clean && npm run production:eleventy && npm run message:dev && run-p --race dev:eleventy dev:parcel:https",
    "dev:eleventy": "eleventy --input=./site --output=./cache_eleventy --watch",
    "dev:parcel": "parcel ./cache_eleventy/* --out-dir ./cache_parcel",
    "dev:parcel:https": "parcel ./cache_eleventy/* --out-dir ./cache_parcel --https",
    "message:dev": "echo 'Your site will be available at: http://127.0.0.1:1234/index.html'",
    "production": "npm run clean && npm run production:eleventy && npm run production:parcel",
    "production:eleventy": "eleventy --input=./site --output=./cache_eleventy",
    "production:parcel": "parcel build ./cache_eleventy/* --out-dir ./dist --no-source-maps",
    "production:pwa": "npm run clean && npm run production:eleventy && npm run production:parcel && workbox injectManifest workbox-config.js && workbox copyLibraries dist/ && mv $(find ./dist -name \\*workbox\\* -type d -maxdepth 1 -print | head -n1) ./dist/workbox",
    "serve": "serve ./dist/"
  },
  "author": "Kolja Kutschera",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^0.8.2",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.18.0",
    "serve": "^11.0.0",
    "workbox-cli": "^4.2.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
